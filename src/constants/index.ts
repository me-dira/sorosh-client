/* eslint-disable no-unused-vars */
export namespace Constants {
  export enum SSE {
    uri = 'https://bot.sapp.ir/v2/#token/getMessage',
  }

  export enum SSE_HEADERS {
    accept = 'application/stream+json',
    contentType = 'application/stream+json',
  }
}
